import React from 'react';
import Logo from './../../Logo/Logo';
import './Toolbar.css';

const toolbar = () => (
  <header className="toolbar">
    <div>Menu</div>
    <Logo />
    <nav>...</nav>
  </header>
);

export default toolbar;
